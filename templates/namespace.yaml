apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace }}
  annotations:
    # Keep the namespace on `helm uninstall` so users do not accidentally wipe
    # adjacent resources (e.g. databases, secrets) that share the namespace.
    "helm.sh/resource-policy": keep